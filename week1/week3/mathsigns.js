// math.round ;if number is above 5 rounds it up if number is below 5 rounds it down
// math.ceil rounds the number up no matter the decimal
// math.floor rounds the number down nomatter the decimal
// math.trunc removes the decimal
// math.min returns the smallest number in a set not array
// math.max returns the highest number in a set not array
// math.random generates random number between 0 and  *multiply it by  the range u want
