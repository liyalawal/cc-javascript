 function numberSplit(n) {
        return [Math.floor(n/2), Math.ceil(n/2)]
    };
  
  console.log(numberSplit(77))
//   math.floor rounds the number DOWN to the nearest integer while math.ceil rounds the number UP to the nearest integer